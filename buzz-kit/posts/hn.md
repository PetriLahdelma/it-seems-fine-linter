Show HN: it-seems-fine-linter — detect vibes-based engineering and score the risk

it-seems-fine-linter scans files for vibes-based engineering phrases, outputs a risk score, and supports strict CI mode. Quickstart:

```bash
npx it-seems-fine-linter --paths src
```

Custom phrase packs are supported via `--config`.
