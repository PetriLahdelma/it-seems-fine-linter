# FAQ

**Does it scan code strings?**
No. It only scans comments and PR metadata, so you can keep string content safe.

**How do I customize phrases?**
Use `--config` with a JSON file or start from `phrase-packs/`.

**Can I fail CI automatically?**
Yes. Run with `--strict` and set `--threshold` to your desired cutoff.
