# Examples

Example inputs and expected outputs for local verification.

- `input/sample.ts` contains two risky phrases.
- `output/score.json` is the expected `--json` output when run with `--strict`.
